apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gateway-test
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  project: default
  source:
    repoURL: https://github.com/KUBE-POD404/opslora-helm-charts.git
    targetRevision: refactor/opslora-helm
    path: opslora-helm/infra/gateway
    helm:
      releaseName: gateway-test
      valueFiles:
        - values.yaml
        - values-test.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: opslora-gateway-ns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
